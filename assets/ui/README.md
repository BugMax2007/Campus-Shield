# UI Assets

- `fonts/`: optional bundled UI fonts for bilingual campus signage rendering.
- `icons/`: reserved for future icon sprites if the current line-icon drawing is replaced.
- `textures/`: reserved for panel grain / background texture assets.

The runtime currently prefers bundled fonts when present and falls back to system fonts on macOS.
